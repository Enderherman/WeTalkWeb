# WeTalkWeb

WeTalkWeb 是 WeTalk 的浏览器客户端新仓库，使用 Vue 3 + TypeScript + Vite。项目独立于 Electron 桌面客户端；后端 API 继续由同级 backend 提供。

## 当前进度

- 已搭建 Vue/Vite 工程和响应式 ChatGPT 风格页面基础。
- 已实现注册、登录、图片验证码、个人资料读取、修改密码、登出和认证后聊天页占位壳，支持桌面和窄屏布局。
- 已接入 HttpOnly Cookie 会话、一次性 WebSocket 票据、同源 WebSocket、5 秒心跳、断线重连、INIT 会话初始化、一对一文字收发、历史游标分页、纯文字 IndexedDB 缓存、消息时间分隔，以及联系人搜索/申请、好友目录、群聊目录/创建、成员名单、群成员管理、群资料编辑、群实时同步、普通文件选择/拖放/上传/下载、聊天图片/音视频预览和用户/群头像及封面显示；收到实时申请帧时更新侧栏计数。
- 140 项认证/资料/密码/路由/聊天/缓存/联系人/群聊/文件/头像/API 错误页/OpenAPI 契约单元测试通过；类型检查和生产构建通过。Vite 代理和临时本地账号完成 HttpOnly Cookie 认证闭环、Cookie 保护的资料请求、短期 WebSocket ticket/一次性握手/登出撤销、联系人搜索/申请/同意、群号搜索/申请/审批、好友资料、删除/拉黑、群创建 multipart 上传、群主添加/移出成员、成员退群、群主解散、群主修改群资料、群主/成员目录与成员名单、双账号文字发送/接收、历史读回、HTTP 过期码 901 和服务端断开/恢复验证；双账号真实 WebSocket 验证群创建/加入/改名/退群/移出/解散事件和成员人数，重连后失效群不再出现在会话列表；普通文件与图片、MP4 视频 type 5/6 上传、历史读回和双方下载字节已真实后端验证，群资料更新后从详情和群主列表读回成功，未上传新头像时原头像文件哈希不变；另验证用户/群头像封面、管理员 `/admin/loadUser` 权限/脱敏，以及更新包发布下载/删除。临时账号、Redis ticket、更新包记录和文件、群、附件、头像和关联数据已清理。管理员和更新包 E2E 清单见对应小节；尚未完成自动化浏览器端到端和多视口视觉验收。

### 首个交付相对空仓库的变化

- 新增可运行的 Vue 3 + TypeScript + Vite 浏览器应用、登录/注册路由和受保护的聊天入口。
- 接入现有账号验证码、注册、登录和登出 API；加入兼容后端的表单校验、会话存储和统一响应处理。
- 完成桌面及手机尺寸的认证页和聊天主框架占位；补充 9 项认证相关测试。
- 当前已有文字消息展示与发送、历史分页、IndexedDB 缓存、联系人搜索/申请、申请收件箱、好友目录、群聊目录/成员名单、群创建表单、群资料编辑、群实时同步、普通文件选择/拖放/上传/下载、聊天图片与音视频预览、用户/群头像与封面显示和群加入申请审批；自动化浏览器端到端仍列入后续阶段。

### 本次更新：个人资料与修改密码

- 登录后从后端读取最新昵称、邮箱和账号编号，个人资料弹窗同步显示。
- 新增修改密码表单，按后端规则校验并确认两次输入；成功后清除会话并要求重新登录。
- 扩充至 17 项认证、资料和密码测试；真实后端验证了旧密码和旧 token 失效、新密码可重新登录。

### 本次更新：404 页面

- 未知地址显示独立的 404 页面，不再被静默转到聊天页。
- 未登录时提供登录入口，已登录时提供返回聊天入口；增加路由与页面测试。

### 本次更新：登录状态过期处理

- 后端返回业务码 901 时，清除本地会话并回到登录页显示重新登录提示。
- 真实后端对无效 token 返回 901；应用处理逻辑和路由提示已通过单元测试。

### 本次更新：实时连接与会话初始化

- 网页端通过同源 `/ws` 连接 Netty，接收会话、最近消息和好友申请计数。
- 每 5 秒发送心跳，连接断开后限次重连；页面卸载时关闭连接。
- 真实后端返回 1 个会话和 1 条初始化消息，关闭连接后心跳键清除。

### 本次更新：一对一文字消息

- 通过现有后端接口发送文字，并在当前会话显示保存结果。
- 接收端通过 WebSocket 即时显示消息，按消息 ID 去重并更新会话摘要。
- 两个本地账号完成好友申请、同意、发送和实时接收验证，临时数据已清理。
- 后端消息长度限制已修正为字符串校验，广播日志不再记录完整消息载荷。

### 本次更新：文字历史分页

- 新增受保护的 `/api/chat/loadHistory`，通过 `beforeMessageId` 游标读取会话历史。
- 网页端首屏载入最近 30 条文字消息，可点按加载更早记录，并维持滚动位置。
- 两个真实本地账号验证 31 条文本的首屏页、游标续页和实时新消息。

### 本次更新：纯文字 IndexedDB 缓存

- 按账号、会话和消息 ID 隔离缓存，只保存文本消息字段。
- 选择会话时先读本机最近页，再由后端分页结果校准；历史页、发送成功和实时接收的文本也会缓存。
- 个人资料面板提供当前账号的本机文字缓存清除入口。

### 本次更新：消息时间分隔

- 连续消息间隔达到 5 分钟或跨日时插入时间分隔。
- 当天显示“今天”，前一天显示“昨天”，更早消息显示年月日和时间。

### 本次更新：消息发送状态

- 请求期间按钮显示“正在发送”并阻止重复提交。
- 收到后端成功响应后，在本人消息旁显示“已发送”；含义是服务端已接收并保存，不表示对方已读。
- 发送失败时保留输入草稿，显示错误并允许再次提交。
- 双账号真实后端联调确认发送成功响应中的消息能由接收方历史接口读回；临时数据已清理。
- 服务端尚无按会话未读数和已读回执字段，未读数与已读状态仍列为后续功能。

### 本次更新：通用 API 错误页

- 网络无响应、服务端 HTTP 5xx 或业务响应中的 5xx 会进入统一服务错误页；页面不展示服务端原始错误细节。
- 可回到触发错误前的页面重试；校验了返回路由，阻止外部地址跳转。
- 901 登录过期仍回登录页，600 业务错误继续由当前表单或功能页展示。
- 真实浏览器通过 Vite 代理验证后端离线时展示错误页、后端恢复后正常载入登录页。

### 本次更新：联系人搜索与好友申请

- 在聊天侧栏添加好友入口，支持按 U 开头的用户编号搜索并展示用户信息和关系状态。
- 支持填写可选验证消息并发送申请；已是好友、被拉黑或已提交申请时会给出相应状态，直接添加成功后重新同步会话。
- 前端 API 与组件测试覆盖有效搜索、找不到用户、好友状态、申请审批和直接添加；真实后端以两个临时账号验证搜索和待处理申请写入，并完成账号及关联数据清理。

### 本次更新：好友申请收件箱

- 在聊天侧栏查看收到的申请数量，打开分页收件箱查看申请人、验证消息和申请时间。
- 支持同意、拒绝或拉黑申请；处理后重新同步联系人与会话。
- 收到实时好友申请帧时更新侧栏申请数；申请记录显示已处理状态，已处理记录不再显示操作按钮。
- 真实后端验证接收方读到待处理申请、同意后双方建立好友关系；测试账号和关联数据已清理。

### 本次更新：好友目录与关系管理

- 新增好友列表与资料查看，资料只展示昵称、编号、性别、地区和签名等需要的字段。
- 删除或拉黑前要求二次确认；完成后重新载入目录并同步聊天会话。
- 真实后端验证两位好友资料可读、拉黑和删除分别返回对应关系状态并从当前目录移除；临时账号和关联数据已清理。

### 本次更新：群聊目录与资料

- 群聊列表合并普通成员加入的群和当前账号拥有的群，选择后显示群主、成员数、加入方式、公告和创建日期。
- 群详情通过 `getGroupInfo4Chat` 展示成员昵称、账号编号和群主标记；当详情对象没有返回成员数时，按成员列表显示。
- 真实后端创建临时群，再让另一个账号直接加入；分别验证群主列表和普通成员列表，双方详情都返回两位成员和成员昵称。测试账号、群记录、会话、消息和头像文件已清理。

### 本次更新：群创建表单

- 新增群名称、群公告、加入方式和 PNG 头像表单；群头像为必填，客户端限制在 10 MB 内。
- 通过 multipart API 创建群聊，成功后刷新群目录并同步聊天会话。
- 单元测试验证表单和 multipart 字段，真实后端验证群创建、直接加入、群主/成员目录与详情后清理临时数据。

### 本次更新：群成员管理

- 群主可以从好友中选择成员并移出成员；普通成员可以退出，群主可以解散群聊。
- 删除成员、退群和解散都需要二次确认；操作成功后刷新群目录、成员名单和聊天会话。
- 真实后端验证群主添加/移出、成员退群和群主解散；相关临时账号和数据已清理。

### 本次更新：群号搜索与申请加入

- 联系人搜索支持以 G 开头的群编号，展示群资料并按加入方式直接加入或提交审批。
- 收件箱区分好友申请和群聊申请；群主同意后，申请人会出现在群成员列表。
- 真实后端验证群号搜索、待审批申请创建和同意后入群；临时账号、群和头像已清理。

### 本次更新：群资料编辑

- 群主可修改群名、公告和加入方式；可选上传 PNG 更换头像，不选择头像时保留当前文件。
- 单元测试覆盖 multipart 更新字段、无头像更新、更换头像字段和编辑表单提交；106 项前端测试、类型检查和生产构建通过。
- 真实后端验证更新结果可从群详情和群主列表读回；省略头像字段后原头像 SHA-256 不变。临时账号、群、头像和关联数据已清理。

### 本次更新：群聊实时同步

- 收到群创建、成员加入、群名修改、退群、移出和解散事件时，更新会话、群名和成员数；群系统消息以居中的事件样式显示。
- 当前账号退群/被移出或群解散后会禁用输入；后端会话索引清理后，重新连接不会重新列出失效群聊。
- 双账号真实后端验证 WebSocket 类型 3、8、9、10、11、12 帧、成员数以及退群/移出/解散后的 INIT；后端 Maven clean verify 的 38 项测试、112 项前端测试、类型检查和生产构建通过。测试数据已清理。

### 本次更新：普通文件上传

- 聊天输入区支持选择或拖放普通文件、图片和音视频；校验文件名、扩展名、空文件及大小（普通文件/音视频 500 MB、图片 200 MB）。
- 先发送 type 5 文件消息元数据，再通过 multipart 上传；展示上传进度和失败状态，并可在当前页面重试。type 6 到达后更新为已上传。
- 单元测试覆盖 fileType 2 元数据、multipart 字段/进度、大小与扩展名校验以及 type 5/6 状态更新；133 项前端测试、类型检查和生产构建通过。
- 双账号真实后端验证发送方和接收方收到消息/完成帧、历史读回、双方下载字节一致；临时账号、群、附件和头像已清理。

### 本次更新：普通文件下载

- 已上传的普通文件显示下载按钮；通过受鉴权的 /chat/downloadFile 获取 Blob，并使用原文件名交给浏览器保存。
- 下载中禁用重复操作；下载错误在附件卡片内显示通用提示。已退群、被移出或已解散的群不允许继续下载。
- 前端组件/API 测试验证下载动作、原文件名、二进制响应和 JSON 业务错误；双账号真实后端已验证授权成员读取的文件字节完整。133 项单测、类型检查和构建通过。

### 本次更新：聊天图片预览

- 常见图片扩展名使用 type 0 上传，客户端限制在 200 MB；上传完成后可以在网页弹层预览。
- 上传完成后显示“预览”入口，通过鉴权下载 API 读取图片 Blob，在弹层内查看；关闭时释放临时对象 URL。
- 前端测试覆盖图片类型识别、大小边界、预览和清理；真实后端验证 type 0 上传完成、历史状态与图片字节读回。当前 133 项单测、类型检查和构建通过。

### 本次更新：音视频播放

- 常见视频/音频扩展名按 type 1 上传；下载后按扩展名设置 MIME，分别使用浏览器 video/audio 控件播放。
- 当前以完整 Blob 下载后播放，未使用 HTTP Range；播放兼容性受浏览器支持的编码格式限制，音视频上限为 500 MB。
- 前端测试验证 type 1 元数据和 video 播放控件；真实后端用小型 MP4 验证上传完成、历史读回和下载字节。133 项前端测试、类型检查和生产构建通过。

### 本次更新：头像与封面显示

- 新增可复用头像组件，通过鉴权 `/chat/downloadFile` 按用户/群编号读取 PNG Blob；用于会话列表、个人账号、联系人搜索/目录、群目录和群成员列表。
- 在个人资料、联系人资料和群资料区域加载封面；图片缺失或下载失败时保留首字母回退/空白封面占位，并在刷新或组件卸载时释放 Blob URL。
- 新增头像下载、失败回退、封面刷新和过期请求竞态测试；138 项前端单测、类型检查和生产构建通过。真实后端临时账号验证头像及封面鉴权下载各一次、字节一致，测试账号与文件已清理。

### 本轮验收：管理员用户列表权限

- 真实后端使用临时普通账号和管理员账号请求 `/admin/loadUser`：未登录返回 901，普通用户返回 404，管理员返回 200。
- 检查管理员列表中的用户记录不包含 `password` 属性；临时账号清理并复核完成。

### 本轮修复：更新包下载契约

- 后端新增受登录态和发布范围校验的 `/app/downloadUpdate`，草稿、非灰度名单用户和外链更新不能当作本地包下载。
- Electron 下载请求改为 `application/x-www-form-urlencoded`，`deleteUpdate` 路径去掉尾随空格。
- 后端 43 项 Maven 测试、WeTalkWeb 138 项测试/类型检查/构建和 Electron 构建通过。真实后端上传并发布临时包后，普通用户下载字节匹配；草稿下载被拒绝，草稿删除成功，临时账号/记录/文件已清理。

### 本轮实现：浏览器安全会话

- 浏览器使用 `/account/webLogin` 登录；响应 JSON 只含公开账号资料，认证 token 通过 HttpOnly、SameSite Strict Cookie 保存。HTTPS 部署设置 `WETALK_WEB_AUTH_COOKIE_SECURE=true`。
- 每次 WebSocket 初连/重连都通过受 Cookie 保护的 `/account/webSocketTicket` 申请 60 秒一次性票据；服务端用 Redis Lua 脚本原子消费。Electron 继续使用旧 token 头和 WebSocket URL 兼容方式。
- 后端 49 项 Maven 测试、前端 140 项测试/类型检查/构建通过。真实后端验证 Cookie 资料请求、WS INIT、ticket 重放被拒、登出撤销和旧 Cookie 返回 901；测试账号、Cookie token 和票据均清理。

## Phase 0：当前网页接口契约

- `docs/openapi.web.json` 使用 OpenAPI 3.2.1，覆盖后端 0.0.2 的全部 46 个 REST 接口；包含可选文件封面、消息文件和更新包下载流及管理员用户敏感字段约束。
- 文档说明 `/api` 同源前缀、`token` 请求头、URL-encoded 表单、业务响应码和当前登录密码 MD5 兼容协议。
- [WebSocket 协议说明](docs/websocket-protocol.md)记录类型 0–16、代表性帧、心跳/重连机制和群事件真实联调；[数据库 ER 图](docs/database-erd.md)来自后端空表 SQL 并区分业务关联与数据库外键。管理员列表角色和敏感字段的真实后端回归已通过，见 Phase 0 记录。
- [浏览器支持与文件规则](docs/browser-support.md)记录桌面/平板/手机验收目标、文件上限、媒体扩展名和浏览器权限要求；矩阵仍需真机视觉验收。
- 主要中性色、强调色、状态色、背景和边框已集中为 `:root` CSS tokens；视觉验收仍需浏览器截图检查。

## 技术栈

- Vue 3、TypeScript、Composition API。
- Vite、Vue Router、Pinia。
- Axios 调用现有 Spring Boot API；浏览器请求使用 URL-encoded 表单，文件上传后续使用 multipart。
- Element Plus 已作为后续表格/对话框控件基础安装；认证页当前用原生表单控件和自定义样式。
- Vitest + Vue Test Utils 做前端单元/组件测试；Playwright 浏览器端到端测试后续加入。
- 浏览器登录 token 不写入 JavaScript 可读存储：`/account/webLogin` 设置 HttpOnly SameSite Strict Cookie，生产 HTTPS 通过 `WETALK_WEB_AUTH_COOKIE_SECURE=true` 启用 Secure；WebSocket 握手使用 60 秒一次性票据。Electron 保留旧 token 头和查询参数兼容。

## 本地开发

当前已验证 Node.js v24.14.0，运行时位于 `D:/environment/node-v24.14`；package.json 的 engines 接受 `^22.18.0 || >=24.12.0`。新装或升级的 Node、Python、JDK、MySQL 等运行环境遵循工作区约定，放在 `D:/environment`；项目 npm 依赖仍安装在本仓库的 `node_modules`。

~~~sh
npm ci
npm run dev
npm run test:unit -- --run
npm run type-check
npm run build
~~~

开发服务器将 /api 代理到 http://127.0.0.1:5050，将 /ws 代理到 ws://127.0.0.1:5051。需要后端及其 MySQL、Redis 依赖运行，页面才能完成真实账号注册和登录。

默认 API 地址为同源 /api。如需连接其他环境，复制 .env.example 为 .env.local 并设置 VITE_API_BASE_URL；不要把密钥放进 VITE_ 变量，因为这些值会被打包进浏览器。

.env.example 当前只设置 VITE_API_BASE_URL=/api。该变量控制 REST 请求；WebSocket 仍走同源 /ws，由 Vite 代理目标或生产反向代理配置。后端运行变量模板和默认值见同级 backend 仓库的 .env.example 与 README 环境变量表。

## 与现有账号接口的兼容

- 验证码接口为 /api/account/checkCode，响应字段为 check_code 和 check_code_key。
- 注册把原始密码通过 HTTPS 表单发给后端；后端沿用当前逻辑保存其 MD5 摘要。
- 登录按现有 Electron 客户端协议发送 MD5(password)。这是兼容旧后端的过渡协议，不是最终密码存储方案。
- sessionStorage 仅保存当前标签页所需的账号公开资料，不保存登录 token；后端 HttpOnly Cookie 管理 REST 登录态，WebSocket 票据 60 秒单次有效。
- 当前账号注册使用图片验证码，没有邮件验证码。

## 设计与开发规则

- 页面优先适配桌面与手机浏览器，侧栏在窄屏折叠为抽屉。
- 视觉参考 ChatGPT 网页聊天页的侧栏、居中对话区、底部输入区域和留白层级，使用 WeTalk 自己的品牌元素。
- API 返回数据以服务端为准；文字缓存接入前先完成后端分页历史接口。
- 不在仓库提交数据库密码、AI API Key 或真实用户数据。
